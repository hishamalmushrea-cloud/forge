# سجل البناء (2026-09-15) — تنفيذ افتراضي + تجميع فيزيائي بيد المستخدم

## ما نُفذ فعليًا هنا (VIRTUAL BUILD — منفذ ومُختبر)
1. مراجعة ستاتيكية للكود ← اكتشاف عيب: INA219 القياسي (3.2A) لا يقيس 4A ← إصلاح v0.2 (‏ACS712-20A + مقسم جهد + تحديث BOM والتوصيل والمعمارية).
2. رسم التجميع `assembly.svg` (مسقط جانبي بالأبعاد + شريط التوصيل) ← متحقق XML.
3. بناء `acceptance.py` (فاحص T2/T3/T4 آلي من CSV + الحجوم).
4. تشغيل `simulated_run.py`: حالة اسمية 30 دقيقة ← PASS ‏(918mL متوقع، خطأ 3%)؛ حالة تسريب ← FAIL مُكتشف ← V1+V2 ‏[VERIFIED].
5. فحص صياغة كل سكربتات Python ← V4 ‏[VERIFIED].

## ما تبقى (PHYSICAL — يحتاج يدك والمكونات)
6. شراء BOM ← 7. تجميع حسب `hardware_guide.md` + الرسم ← 8. معايرة ACS712 والمقسم ← 9. رفع `measurement.ino` ← 10. تنفيذ T1–T5 ← 11. إرسال CSV والحجوم لأشغّل `acceptance.py` عليها.

## تنبيه صدق
البيانات المحاكاة (sim_*.csv) تختبر خط التحقق فقط ولا تثبت أي فيزياء — الإثبات الفيزيائي الوحيد هو T2–T4 على العتاد الحقيقي.
