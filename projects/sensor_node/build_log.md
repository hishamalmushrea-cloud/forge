# سجل البناء (2026-09-15) — عقدة الحساس

## ما نُفذ فعليًا هنا (VIRTUAL + HOST BUILD)
1. برنامج `firmware/` (منطق واعٍ بالطاقة، مستقل عن العتاد) ← تُرجم بـgcc دون تحذيرات ← 14/14 اختبار مضيف ناجح:
   بوابة الإقلاع، عتبة 2.3V، مضاعفة النوم، 3 سيناريوهات طاقة (طازج/متقادم/جائع) — كلها تحترم حد 2.0V.
2. عيب حقيقي اكتُشف وأُصلح: عتبة 2.2V تهبط بالمكثف لـ1.96V بعد الرشقة ← رُفعت لـ2.3V في الكود والدليل والتقرير والمخطط.
3. `acceptance_t4.py` (فاحص أسبوع التشغيل) ← اسمي PASS وعطل الهبوط مُكتشف FAIL ‏[VERIFIED].
4. مخطط التوصيل `assembly_wiring.svg` ← متحقق.
5. (أخطائي المسجلة: توقع اختبار خاطئ صُحح، سطر بلا `def` صُلح — الاختبارات أمسكت الاثنين.)

## المتبقي (يحتاج عتادك)
6. بناء الإلكترونيات + T1–T5 بالمصدر البديل ← 7. إرسال CSV الأسبوع لأشغّل عليه الفاحص.
البرنامج: COMPILED + HOST-TESTED (ترقية من pseudo-C) — بورت المتحكم المحدد NOT EXECUTED.
